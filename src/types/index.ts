// we import the types to re-export them inside types.
import {
  DatePrimitive,
  array,
  boolean,
  compose,
  custom,
  enumeration,
  finite,
  float,
  frozen,
  identifier,
  identifierNumber,
  integer,
  late,
  lazy,
  literal,
  map,
  maybe,
  maybeNull,
  model,
  nullType,
  number,
  optional,
  reference,
  refinement,
  safeReference,
  snapshotProcessor,
  string,
  undefinedType,
  union
} from "../internal.ts"

export const types = {
  enumeration,
  model,
  compose,
  custom,
  reference,
  safeReference,
  union,
  optional,
  literal,
  maybe,
  maybeNull,
  refinement,
  string,
  boolean,
  number,
  integer,
  float,
  finite,
  Date: DatePrimitive,
  map,
  array,
  frozen,
  identifier,
  identifierNumber,
  late,
  lazy,
  undefined: undefinedType,
  null: nullType,
  snapshotProcessor
}
